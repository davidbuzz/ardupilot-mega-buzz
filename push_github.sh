# push the main branch only...? 
git push github_buzz master

# if you get a 'non-fast-forward updates were rejected' message, it's becasue someone else has 
# pushed since you pulled.     just do another pull ( or fetch then merge ) then try pushing again.

